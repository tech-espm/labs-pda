<html xmlns="http://www.w3.org/1999/xhtml" lang="pt-BR">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />	
	<title>Jogo</title>
	<style >

	body {
		height:100%;
		background-position: center;
		background-repeat: no-repeat;
		background-size: contain;
	}

	.titulo{
		text-align: center;
	}
	</style>
</head>
<%
  for (let i = 0; i < estados.length; i++) {
	if (estados[i].inicial == 1) {
%>
<body style="background-image: url(/public/img/estados/<%=estados[i].id%>.jpg)">

	<h1 class="titulo"> <%=estados[i].descricao%></h1>

	<%
		let textos = [
			(estados[i].texto1 || "").normalize(),
			(estados[i].texto2 || "").normalize(),
			(estados[i].texto3 || "").normalize(),
			(estados[i].texto4 || "").normalize(),
			(estados[i].texto5 || "").normalize()
		];
		let preenchidos = 0;

		for (let i = textos.length - 1; i >= 0; i--) {
			if (!textos[i]) {

			}else{
				preenchidos++;
			}
		}

  		for (let j = 0; j < preenchidos ; j++) {		
	%>
		<div>
			<p> <%=textos[j]%> </p>
		</div>
	<%		
  	  }
	%>







<%
	}
  }
%>
	<script type="text/javascript">
    </script>	

</body>
</html>